<UserControl x:Class="Modelisateur.Modules.DbExplorer.Views.DbExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:control="clr-namespace:Gemini.Modules.Explorer.Controls;assembly=Gemini.Modules.Explorer"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Content="Connect to database" Command="{Binding OpenSourceCommand}" />
        <control:ExplorerControl Grid.Row="1" SourceTree="{Binding SourceTree}" 
                                 ContextMenuModel="{Binding ContextMenuModel}" />
    </Grid>
</UserControl>

